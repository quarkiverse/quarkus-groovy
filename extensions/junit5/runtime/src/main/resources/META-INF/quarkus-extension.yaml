name: "Groovy JUnit 5 for integration tests"
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  keywords:
  - "groovy"
  - "junit5"
  - "testing"
  categories:
  - "test"
  status: "preview"
